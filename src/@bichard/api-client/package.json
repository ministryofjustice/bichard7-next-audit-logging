{
  "name": "@bichard/api-client",
  "description": "Provides a higher level set of abstractions to work with Audit Log Api",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "@bichard/testing": "file:../testing",
    "@types/jest": "^26.0.24",
    "@typescript-eslint/eslint-plugin": "^4.9.0",
    "@typescript-eslint/parser": "^4.9.0",
    "eslint": "^7.15.0",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "https": "^1.0.0",
    "shared": "file:../../../shared"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "setup:env": "cd ../../../environment && ./setup.sh && cd -",
    "test": "npm run setup:env && ../../../scripts/test.sh",
    "test:ci": "npm run setup:env && ../../../scripts/test.sh ci",
    "test:unit": "../../../scripts/test.sh unit",
    "test:integration": "npm run setup:env && ../../../scripts/test.sh integration",
    "test:e2e": "npm run setup:env && ../../../scripts/test.sh e2e"
  }
}
