{
  "name": "@handlers/common",
  "description": "Hosts common types and services",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.9.0",
    "@typescript-eslint/parser": "^4.9.0",
    "aws-sdk": "^2.804.0",
    "eslint": "^7.15.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "cp package.json dist/package.json && cd dist && npm i --production",
    "test": "jest",
    "lint": "npx eslint src/**/*.ts"
  }
}
