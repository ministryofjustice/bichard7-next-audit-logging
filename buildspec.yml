---

version: 0.2

phases:
  install:
    commands:
      - yum install -y zip
  build:
    commands:
      - scripts/install-all.sh
      - scripts/build-all.sh
      - scripts/codebuild-artifacts.sh
...
